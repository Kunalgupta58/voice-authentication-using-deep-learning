# Web framework
fastapi>=0.110.0
uvicorn>=0.29.0
gunicorn>=21.2.0
python-multipart>=0.0.9
slowapi==0.1.4

# Database
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Audio
pydub>=0.25.1
soundfile>=0.12.1

# Numpy — MUST be < 2.0 for PyTorch 2.2.x + SpeechBrain compatibility
numpy>=1.24.0,<2.0.0

# ML — all pinned so rebuilds are reproducible
torch==2.2.2
torchaudio==2.2.2
speechbrain>=1.0.0,<2.0.0
huggingface-hub>=0.23.2,<1.0.0
scipy>=1.11.0,<2.0.0
librosa>=0.10.1,<0.11.0
faiss-cpu>=1.7.4

# Utils
requests
python-dotenv
